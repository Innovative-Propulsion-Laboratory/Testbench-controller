#ifndef SEQUENCE_H
#define SEQUENCE_H

// Don't include other headers here
#include <stdint.h>

void set_offset_pressure();
float average(int32_t* L, int length);
void Sequence();
void count_down();

#endif